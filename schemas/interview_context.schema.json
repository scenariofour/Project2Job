{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://project2job.dev/schemas/interview-context/1.0.0",
  "title": "InterviewContext",
  "description": "Company, track, and reported interview context for one target JD, gathered by bounded automatic public-web research and from any material the user supplies. Research about a company is never project evidence: every item carries a source status and is kept separate from verified project facts.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "company",
    "research",
    "signals",
    "questions",
    "unknowns"
  ],
  "properties": {
    "schema_version": {
      "const": "1.0.0"
    },
    "company": {
      "type": "string",
      "minLength": 1
    },
    "track": {
      "type": "string",
      "description": "Team, product area, or track. Omit when unknown; do not guess."
    },
    "level": {
      "type": "string"
    },
    "location": {
      "type": "string"
    },
    "research": {
      "$ref": "#/$defs/researchRun"
    },
    "signals": {
      "type": "array",
      "minItems": 0,
      "items": {
        "$ref": "#/$defs/interviewSignal"
      }
    },
    "questions": {
      "type": "array",
      "minItems": 0,
      "items": {
        "$ref": "#/$defs/interviewQuestion"
      }
    },
    "conflicts": {
      "type": "array",
      "description": "Pairs of signal or question IDs that disagree, with the unresolved difference stated.",
      "items": {
        "$ref": "#/$defs/reportConflict"
      }
    },
    "unknowns": {
      "type": "array",
      "description": "What could not be established. An empty company track must appear here rather than be inferred.",
      "items": {
        "type": "string"
      }
    }
  },
  "$defs": {
    "sourceStatus": {
      "description": "How well established an interview claim is. There is no value meaning 'certain'.",
      "enum": [
        "official",
        "repeatedly_reported",
        "single_report",
        "inferred_from_jd",
        "unknown"
      ]
    },
    "freshness": {
      "enum": [
        "fresh",
        "aging",
        "stale",
        "unknown"
      ]
    },
    "presentedAs": {
      "description": "Strength of language the output may use. No value permits presenting a question as guaranteed.",
      "enum": [
        "likely",
        "possible",
        "reported_once",
        "speculative"
      ]
    },
    "interviewLayer": {
      "description": "Which of the three explicit layers this item belongs to. Personality-based culture fit is not a layer.",
      "enum": [
        "company_interview_signal",
        "track_team_level_requirement",
        "reported_interview_evidence"
      ]
    },
    "researchSource": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "origin",
        "reference"
      ],
      "properties": {
        "origin": {
          "description": "Where the material came from. Public-web origins are retrieved by bounded automatic research; user origins are supplied directly. Both are supported.",
          "enum": [
            "official_company_page",
            "official_company_material",
            "public_report_page",
            "job_description",
            "user_pasted_report",
            "user_uploaded_file",
            "user_own_experience"
          ]
        },
        "url": {
          "type": "string",
          "description": "Required for public-web origins. The exact page the claim came from, not a domain or a search result list."
        },
        "fetch_method": {
          "$ref": "#/$defs/fetchMethod"
        },
        "retrieved_on": {
          "type": "string",
          "description": "ISO-8601 date the page was retrieved. Distinct from source_date, which is when the content was written."
        },
        "reference": {
          "type": "string",
          "minLength": 1,
          "description": "Exact locator inside the supplied material."
        },
        "source_date": {
          "type": "string",
          "description": "ISO-8601 date the material was published or the experience occurred. Absent means unknown, which forces freshness 'unknown'."
        },
        "interview_stage": {
          "enum": [
            "recruiter_screen",
            "hiring_manager",
            "technical_or_case",
            "cross_functional",
            "final_or_bar_raiser",
            "unknown"
          ]
        }
      },
      "allOf": [
        {
          "description": "A web-retrieved claim must cite the exact page, how it was fetched, and when.",
          "if": {
            "properties": {
              "origin": {
                "enum": [
                  "official_company_page",
                  "public_report_page"
                ]
              }
            },
            "required": [
              "origin"
            ]
          },
          "then": {
            "required": [
              "url",
              "fetch_method",
              "retrieved_on"
            ]
          }
        }
      ]
    },
    "fetchMethod": {
      "description": "How a page was read. Playwright is a real MVP capability but is an escalation, not the default.",
      "enum": [
        "read_only_fetch",
        "playwright",
        "cache"
      ]
    },
    "sourceTier": {
      "description": "Drives official-first prioritization. Official pages are fetched and trusted before independent reports.",
      "enum": [
        "official",
        "independent_report",
        "aggregator_or_forum",
        "unknown"
      ]
    },
    "researchRun": {
      "type": "object",
      "additionalProperties": false,
      "description": "One bounded research pass. Present even when no research ran, so a thin company brief is explained rather than unexplained.",
      "required": [
        "mode",
        "budget",
        "queries",
        "pages",
        "stop_reason",
        "gaps"
      ],
      "properties": {
        "mode": {
          "enum": [
            "automatic_bounded",
            "user_supplied_only",
            "unavailable"
          ]
        },
        "budget": {
          "$ref": "#/$defs/researchBudget"
        },
        "queries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/searchQuery"
          }
        },
        "pages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/researchPage"
          }
        },
        "stop_reason": {
          "$ref": "#/$defs/researchStopReason"
        },
        "gaps": {
          "type": "array",
          "description": "What the research could not establish. Drives the unknowns list rather than an inference.",
          "items": {
            "type": "string"
          }
        }
      },
      "allOf": [
        {
          "description": "Automatic research must show its work: at least one query and one page record.",
          "if": {
            "properties": {
              "mode": {
                "const": "automatic_bounded"
              }
            },
            "required": [
              "mode"
            ]
          },
          "then": {
            "properties": {
              "queries": {
                "minItems": 1
              },
              "pages": {
                "minItems": 1
              }
            }
          }
        },
        {
          "description": "When no research ran, the run cannot claim it stopped because the evidence was sufficient.",
          "if": {
            "properties": {
              "mode": {
                "enum": [
                  "user_supplied_only",
                  "unavailable"
                ]
              }
            },
            "required": [
              "mode"
            ]
          },
          "then": {
            "properties": {
              "stop_reason": {
                "enum": [
                  "research_not_run",
                  "tool_failure"
                ]
              }
            }
          }
        }
      ]
    },
    "researchBudget": {
      "type": "object",
      "additionalProperties": false,
      "description": "Hard ceilings. The maximums here are the contract; docs/09_TOKEN_CONTEXT_AND_COST.md explains them. A run may declare less, never more.",
      "required": [
        "max_search_queries",
        "max_pages_fetched",
        "max_playwright_pages",
        "max_navigation_depth",
        "max_chars_per_page",
        "max_total_tokens",
        "max_retries_per_page",
        "max_runtime_seconds"
      ],
      "properties": {
        "max_search_queries": {
          "type": "integer",
          "minimum": 1,
          "maximum": 8
        },
        "max_pages_fetched": {
          "type": "integer",
          "minimum": 1,
          "maximum": 12
        },
        "max_playwright_pages": {
          "type": "integer",
          "minimum": 0,
          "maximum": 3
        },
        "max_navigation_depth": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1,
          "description": "0 means fetch only the search result URL. 1 permits one in-page link on the same host. Never crawl."
        },
        "max_chars_per_page": {
          "type": "integer",
          "minimum": 1,
          "maximum": 20000
        },
        "max_total_tokens": {
          "type": "integer",
          "minimum": 1,
          "maximum": 60000
        },
        "max_retries_per_page": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1
        },
        "max_runtime_seconds": {
          "type": "integer",
          "minimum": 1,
          "maximum": 120
        }
      }
    },
    "searchQuery": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "query",
        "purpose",
        "results_considered",
        "results_kept"
      ],
      "properties": {
        "query": {
          "type": "string",
          "minLength": 1
        },
        "purpose": {
          "description": "Which gap this query was meant to close. A follow-up query is only justified by a named gap.",
          "enum": [
            "official_interview_signals",
            "track_team_level_expectations",
            "reported_interview_process",
            "reported_interview_questions",
            "conflict_or_recency_check"
          ]
        },
        "results_considered": {
          "type": "integer",
          "minimum": 0
        },
        "results_kept": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "researchPage": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "url",
        "tier",
        "outcome"
      ],
      "properties": {
        "url": {
          "type": "string",
          "minLength": 1
        },
        "canonical_url": {
          "type": "string",
          "description": "Normalized URL used for deduplication."
        },
        "tier": {
          "$ref": "#/$defs/sourceTier"
        },
        "fetch_method": {
          "$ref": "#/$defs/fetchMethod"
        },
        "escalation_reason": {
          "description": "Why a plain read-only fetch was not enough. Required when fetch_method is playwright.",
          "enum": [
            "javascript_rendered",
            "navigation_required",
            "structure_required"
          ]
        },
        "outcome": {
          "enum": [
            "extracted",
            "duplicate_of_kept_page",
            "inaccessible_login_required",
            "inaccessible_blocked",
            "render_required",
            "fetch_failed",
            "skipped_budget",
            "skipped_irrelevant"
          ]
        },
        "retrieved_on": {
          "type": "string"
        },
        "chars_retained": {
          "type": "integer",
          "minimum": 0
        },
        "duplicate_of": {
          "type": "string",
          "description": "canonical_url of the page this duplicates."
        }
      },
      "allOf": [
        {
          "description": "Playwright use must be justified per page.",
          "if": {
            "properties": {
              "fetch_method": {
                "const": "playwright"
              }
            },
            "required": [
              "fetch_method"
            ]
          },
          "then": {
            "required": [
              "escalation_reason"
            ]
          }
        },
        {
          "description": "A duplicate must name what it duplicates, and retain nothing.",
          "if": {
            "properties": {
              "outcome": {
                "const": "duplicate_of_kept_page"
              }
            },
            "required": [
              "outcome"
            ]
          },
          "then": {
            "required": [
              "duplicate_of"
            ],
            "properties": {
              "chars_retained": {
                "maximum": 0
              }
            }
          }
        },
        {
          "description": "A login wall is recorded and abandoned. It is never retried with credentials, and nothing is extracted from it.",
          "if": {
            "properties": {
              "outcome": {
                "const": "inaccessible_login_required"
              }
            },
            "required": [
              "outcome"
            ]
          },
          "then": {
            "properties": {
              "chars_retained": {
                "maximum": 0
              }
            }
          }
        },
        {
          "description": "Only an extracted page may retain content.",
          "if": {
            "properties": {
              "outcome": {
                "const": "extracted"
              }
            },
            "required": [
              "outcome"
            ]
          },
          "then": {
            "required": [
              "fetch_method",
              "retrieved_on",
              "chars_retained"
            ]
          }
        }
      ]
    },
    "researchStopReason": {
      "description": "Mirrors the Day 1 loop vocabulary where the meaning is the same.",
      "enum": [
        "evidence_sufficient",
        "evidence_exhausted",
        "budget_exhausted",
        "sources_inaccessible",
        "conflict_requires_disclosure",
        "tool_failure",
        "research_not_run"
      ]
    },
    "interviewSignal": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "signal_id",
        "layer",
        "statement",
        "source_status",
        "sources",
        "freshness"
      ],
      "properties": {
        "signal_id": {
          "type": "string"
        },
        "layer": {
          "$ref": "#/$defs/interviewLayer"
        },
        "statement": {
          "type": "string",
          "minLength": 1
        },
        "source_status": {
          "$ref": "#/$defs/sourceStatus"
        },
        "sources": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/researchSource"
          }
        },
        "freshness": {
          "$ref": "#/$defs/freshness"
        },
        "company": {
          "type": "string"
        },
        "track": {
          "type": "string"
        },
        "level": {
          "type": "string"
        },
        "location": {
          "type": "string"
        }
      },
      "allOf": [
        {
          "description": "Anything above inferred_from_jd must cite at least one supplied source.",
          "if": {
            "properties": {
              "source_status": {
                "enum": [
                  "official",
                  "repeatedly_reported",
                  "single_report"
                ]
              }
            },
            "required": [
              "source_status"
            ]
          },
          "then": {
            "properties": {
              "sources": {
                "minItems": 1
              }
            }
          }
        },
        {
          "description": "repeatedly_reported requires more than one independent source.",
          "if": {
            "properties": {
              "source_status": {
                "const": "repeatedly_reported"
              }
            },
            "required": [
              "source_status"
            ]
          },
          "then": {
            "properties": {
              "sources": {
                "minItems": 2
              }
            }
          }
        }
      ]
    },
    "interviewQuestion": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "question_id",
        "text",
        "layer",
        "source_status",
        "presented_as",
        "priority",
        "sources",
        "freshness"
      ],
      "properties": {
        "question_id": {
          "type": "string"
        },
        "text": {
          "type": "string",
          "minLength": 1
        },
        "layer": {
          "$ref": "#/$defs/interviewLayer"
        },
        "source_status": {
          "$ref": "#/$defs/sourceStatus"
        },
        "presented_as": {
          "$ref": "#/$defs/presentedAs"
        },
        "priority": {
          "enum": [
            "P0",
            "P1"
          ]
        },
        "sources": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/researchSource"
          }
        },
        "freshness": {
          "$ref": "#/$defs/freshness"
        },
        "why_it_is_likely": {
          "type": "string",
          "description": "Which role requirement or signal makes this question plausible."
        },
        "company": {
          "type": "string"
        },
        "track": {
          "type": "string"
        },
        "level": {
          "type": "string"
        },
        "location": {
          "type": "string"
        }
      },
      "allOf": [
        {
          "description": "Anything above inferred_from_jd must cite at least one supplied source.",
          "if": {
            "properties": {
              "source_status": {
                "enum": [
                  "official",
                  "repeatedly_reported",
                  "single_report"
                ]
              }
            },
            "required": [
              "source_status"
            ]
          },
          "then": {
            "properties": {
              "sources": {
                "minItems": 1
              }
            }
          }
        },
        {
          "description": "repeatedly_reported requires more than one independent source.",
          "if": {
            "properties": {
              "source_status": {
                "const": "repeatedly_reported"
              }
            },
            "required": [
              "source_status"
            ]
          },
          "then": {
            "properties": {
              "sources": {
                "minItems": 2
              }
            }
          }
        },
        {
          "description": "A question may only be called fresh or aging when at least one source carries a date.",
          "if": {
            "properties": {
              "freshness": {
                "enum": [
                  "fresh",
                  "aging"
                ]
              }
            },
            "required": [
              "freshness"
            ]
          },
          "then": {
            "properties": {
              "sources": {
                "contains": {
                  "required": [
                    "source_date"
                  ]
                }
              }
            }
          }
        },
        {
          "description": "One reported experience may never be presented more strongly than 'reported_once'.",
          "if": {
            "properties": {
              "source_status": {
                "const": "single_report"
              }
            },
            "required": [
              "source_status"
            ]
          },
          "then": {
            "properties": {
              "presented_as": {
                "const": "reported_once"
              }
            }
          }
        },
        {
          "description": "A JD inference or an unknown may never be presented as likely.",
          "if": {
            "properties": {
              "source_status": {
                "enum": [
                  "inferred_from_jd",
                  "unknown"
                ]
              }
            },
            "required": [
              "source_status"
            ]
          },
          "then": {
            "properties": {
              "presented_as": {
                "enum": [
                  "possible",
                  "speculative"
                ]
              }
            }
          }
        },
        {
          "description": "A stale report may not be presented as likely.",
          "if": {
            "properties": {
              "freshness": {
                "const": "stale"
              }
            },
            "required": [
              "freshness"
            ]
          },
          "then": {
            "properties": {
              "presented_as": {
                "enum": [
                  "possible",
                  "reported_once",
                  "speculative"
                ]
              }
            }
          }
        }
      ]
    },
    "reportConflict": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "item_ids",
        "disagreement",
        "resolution"
      ],
      "properties": {
        "item_ids": {
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "string"
          }
        },
        "disagreement": {
          "type": "string",
          "minLength": 1
        },
        "resolution": {
          "description": "Conflicting reports are surfaced, not silently averaged or dropped.",
          "enum": [
            "unresolved_both_shown",
            "newer_preferred_both_shown",
            "official_preferred_both_shown"
          ]
        }
      }
    }
  }
}
